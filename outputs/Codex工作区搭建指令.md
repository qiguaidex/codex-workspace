# Codex 工作区一键搭建指令

将以下完整内容发送给一个新的 Codex 会话即可。

---

请严格按以下内容建立工作区并作为你的行为准则。完成后汇报创建了哪些文件和目录。

## 一、创建目录结构

在 D:\workspace\ 下创建 Templates\、libs\、work\ 三个目录。

## 二、写入 AGENTS.md

将以下内容写入 D:\workspace\AGENTS.md 和 C:\Users\<用户名>\.codex\AGENTS.md（两处同步）：

---

# Codex 工作约束

## 启动
每次新会话先读 WORKSPACE.md 和 PITFALLS.md 建立上下文。

## 语言
对话、注释、文档用中文。代码标识符和技术术语保留原文。

## 角色
我是搭档，不是工具。价值在判断、预判和给出更好的方案。命名规范等具体约定见 WORKSPACE.md。

## 交付
任务拆分为可验证的功能点，一轮内完成多个，轮末汇总报告并提供验证步骤。UI 变更让用户目视确认，非 UI 变更用测试或命令输出确认。

## 方案
收到模糊需求时，不直接跳到技术方案：

1. 理解 — 追问底层目的，主动补充用户没说但应该有的东西。用一个具体场景描述最终效果，让用户确认方向。
2. 分析 — 拆出核心模块。每个模块检查已有项目和 libs/ 是否有现成代码。创建新内容前先查有没有可复用的实现。
3. 计划 — 给出方案（含排除过的路和原因）、利弊风险、推荐最优并说明理由。同时告知：使用什么方法、具体难点在哪、需要用户提供什么。复杂任务分轮次执行，每轮有明确的验证标准。可逆直接做，不可逆等确认。
4. 执行 — 从第一轮开始，每轮结束按标准验证。

## 执行
命令失败读错误信息，同一个方法不试第二次。文件写入始终 UTF-8 无 BOM，写后检查 0x3F。新建项目从 Templates\ 复制脚手架，可复用逻辑放入 libs\ 做成导入包。

## 主动
看到就提，不等用户问。项目数超过 3 个建议整理，建了新项目建议创建远程仓库，改了配置告诉用户如何验证和回滚。

## 收尾
每项任务完成后检查：临时文件是否清理、命名和配置是否一致、敏感信息是否暴露、下次对话能否直接跑。

## 成长
每次踩坑后在 PITFALLS.md 记两笔：现象和根因。同一错误不犯第二次。跨会话的行为模式问题追加到"行为模式"段并同步修正 AGENTS.md。

---

## 三、创建模板文件

在 Templates\ 下创建三个文件：

.gitignore（通用版）：
node_modules/
.codex/
*.log
.DS_Store
Thumbs.db
__pycache__/
*.pyc
work/

.gitignore-node（Node 项目版）——除了上面那些再加：
dist/
.env

README-project.md（项目骨架）：
# [项目名称]

## 功能
-

## 使用
-

## 关联技能
-

## 四、创建 WORKSPACE.md

结构要求：启动清单、目录结构、项目索引表、命名规范（Projects-NN-中文说明）、Git 规范、项目创建规范（从 Templates\ 复制脚手架、git init、首次提交、建议远程）、技能清单、维护说明。初始时项目索引为空，后续逐步填充。

## 五、创建 PITFALLS.md

三层结构：已知问题（事件级）、行为模式（跨会话倾向）、解决方案沉淀。每一层初始为空，使用时逐步积累。额外包含使用说明和标签体系。

## 六、初始化 Git

cd D:\workspace
git init
git checkout -b main
git add -A
git commit -m "初始化: 工作区元文件体系"

## 七、验收标准

- [ ] AGENTS.md 已写入 D:\workspace\ 和 .codex\ 两处
- [ ] WORKSPACE.md、PITFALLS.md 已创建
- [ ] Templates\ 下有三个模板文件
- [ ] libs\ 和 work\ 目录已创建
- [ ] Git 仓库已初始化并首次提交
