# 全局踩坑日志

> 与 `AGENTS.md`（VIBE CODING 增强规则）配合使用：AGENTS.md 定义「该怎么做」，本文件记录「做错了什么」。
> Codex AI 在每次新会话时应搜索与当前任务相关的标签，避免重复踩坑。

---

## 使用说明

### 对人类
- 遇到任何错误或异常行为，按下方模板记录
- 即使当时解决了也要记，未来可能复现
- 不确定原因也记录，标注「原因未知」

### 对 AI
- 新会话开始时搜索相关 `#标签`
- 执行任务前检查是否有历史问题
- 遇到新问题，解决后追加到本文件
- 如果发现某个错误模式反复出现，追加到「反模式」部分

---

## 问题模板

```markdown
## YYYY-MM-DD | 项目 | 问题简述

- **标签**: #tag1 #tag2
- **现象**: 实际看到了什么异常
- **原因**: 为什么会发生
- **解决**: 具体做了什么
- **教训**: 如何预防，或者下次怎么更快定位
```

---

## 标签体系

| 类别 | 标签 | 说明 |
|------|------|------|
| Codex | `#codex` `#codex-display` `#codex-config` `#codex-plugin` | Codex 软件本身 |
| Git | `#git` `#git-commit` `#git-remote` `#git-conflict` | 版本控制 |
| 项目 | `#feishu` `#coze` `#wechat` `#douyin` `#rockmoons` | 各项目领域 |
| 系统 | `#windows` `#powershell` `#encoding` `#permission` | 运行环境 |
| Vibe Coding | `#vibe` `#vibe-split` `#vibe-verify` `#vibe-overengineer` | 开发流程问题 |
| 通用 | `#bug` `#workaround` `#wontfix` `#todo` | 通用分类 |

---

## 已知问题记录

### 2026-06-05 | Codex 自身 | Codex 最大化后显示不全

- **标签**: `#codex` `#codex-display` `#wontfix`
- **现象**: Codex 窗口最大化后，界面未能完全填充屏幕
- **原因**: 待确认，可能与高分屏缩放有关
- **解决**: 待排查
- **教训**: 首次遇到时未记录，导致后续重复排查

---

### 2026-06-05 | Codex 自身 | Codex 最大化后边框透明

- **标签**: `#codex` `#codex-display`
- **现象**: 最大化后窗口部分边框变成透明，可见桌面背景
- **原因**: 待确认
- **解决**: 待排查
- **教训**: 与「显示不全」可能同源

---

### 2026-06-04 | Codex 配置 | 插件列表灰色不可用

- **标签**: `#codex` `#codex-plugin`
- **现象**: 插件管理界面中插件灰色，无法启用
- **原因**: 待确认
- **解决**: 通过修改配置文件手动启用
- **教训**: UI 操作无效时直接检查配置文件

---

### 2026-06-04 | Codex 配置 | 上下文窗口扩展到 105 万

- **标签**: `#codex` `#codex-config`
- **现象**: 需要将上下文窗口从默认值扩展到 105 万 token
- **原因**: 默认窗口小，复杂任务不够用
- **解决**: 修改 `config.toml` 中的上下文窗口配置
- **教训**: 修改配置后需重启 Codex，且需自查确认生效

---

### 2026-06-07 | Codex 自身 | 不能以管理员方式运行

- **标签**: `#codex` `#windows` `#permission` `#wontfix`
- **现象**: 用户尝试以管理员权限运行 Codex 失败
- **原因**: Codex 安全限制，不允许管理员权限运行
- **解决**: 用普通用户权限运行，需管理员操作时单独开终端
- **教训**: Codex 有安全沙箱，不要试图绕过

---

### 2026-06-08 | 工作区 | 文件被进程占用无法删除

- **标签**: `#windows` `#workaround`
- **现象**: `Move-Item` / `Remove-Item` 报「文件被其他进程占用」
- **原因**: Codex 或文件管理器持有目录句柄
- **解决**: 用 `robocopy /MOVE` 复制后源目录留空壳，重启后消失
- **教训**: Windows 批量移动文件优先用 `robocopy /MOVE`

---

### 2026-06-06~08 | Git | 多个仓库 init 后未提交

- **标签**: `#git` `#git-commit` `#vibe`
- **现象**: 5 个仓库中 3 个只有 `git init`，零提交
- **原因**: 项目创建初期忘记提交
- **解决**: 逐个完成首次提交，统一整理到 `D:\workspace\`
- **教训**: 新项目创建后应立刻首次提交；这属于 vibe coding 中「只写代码不归档」的典型问题

---

### 2026-06-03~08 | Git | 所有仓库无远程备份

- **标签**: `#git` `#git-remote` `#todo`
- **现象**: 所有仓库纯本地，未关联 GitHub 远程
- **原因**: 未创建 GitHub 仓库
- **解决**: 待创建并推送
- **教训**: 有进展后应立即推远程，防止硬盘故障丢失

---

## Vibe Coding 反模式（重要）

> 以下是在 vibe coding 中容易反复出现的错误工作方式。
> 它们违反了 `AGENTS.md` 的核心原则，每次踩坑后追加。

### 反模式 1：连续编写不验证

- **标签**: `#vibe` `#vibe-verify`
- **现象**: AI 连续写多个文件后才让用户验证，结果前面的代码就有问题，后面全部白写
- **后果**: 错误堆积，排查困难，可能需要大量回退
- **正确做法**: 每完成一个原子功能点就暂停，按交付模板要求用户验证
- **触发信号**: 一次修改超过 3 个文件、新增超过 100 行代码

### 反模式 2：过度工程化

- **标签**: `#vibe` `#vibe-overengineer`
- **现象**: 动手前写长篇设计文档，包含测试计划、错误处理、性能优化等
- **后果**: 浪费时间在不会用到的设计上，真正该写的代码还没开始
- **正确做法**: 先问 A/B，选择 B 直接写代码，或选择 A 写极简文档（3-5 功能点 + 文件结构 + 技术选型）
- **触发信号**: 设计文档超过一页、包含测试策略或部署方案章节

### 反模式 3：假设 bash 成功 = 功能正确

- **标签**: `#vibe` `#vibe-verify`
- **现象**: 命令执行无报错，AI 认为功能正常，直接继续下一步
- **后果**: 命令不报错不等于程序按预期运行，用户实际看到的可能完全不同
- **正确做法**: bash 执行后明确告诉用户如何验证实际效果，等待反馈
- **触发信号**: 执行完 `npm run dev` 或 `python app.py` 后没有要求用户在浏览器/终端验证

### 反模式 4：忽略用户反馈继续写新功能

- **标签**: `#vibe` `#vibe-verify`
- **现象**: 用户报告问题，AI 说「这个问题稍后处理」然后继续写新代码
- **后果**: 问题积累，用户失去信心，项目最终无法运行
- **正确做法**: 用户反馈优先级最高（> bash 输出 > 代码推测），立即停新功能，定位修复

---

## 解决方案沉淀

### 模式：Windows 文件占用处理
```
场景: 文件/目录无法删除或移动，报"被另一程序占用"
步骤:
  1. robocopy <源> <目标> /E /MOVE /R:2 /W:1
  2. 忽略"删除源目录失败"错误
  3. 重启后空壳目录自动消失
```

### 模式：Codex 配置修改
```
场景: 修改 Codex 行为配置
文件: C:\Users\Administrator\.codex\config.toml
要点: 修改前备份 → 修改后重启 → 启动后验证
```

### 模式：新项目初始化清单
```
1. 创建目录（按 Projects-NN-中文说明 格式）
2. git init && git checkout -b main
3. 创建 .gitignore（至少排除 node_modules/ .codex/）
4. 创建 README.md
5. 首次提交
6. 创建 GitHub 仓库并推送
```

### 模式：Vibe Coding 正确节奏
```
每次动手前:
  1. 问 A/B（设计文档 or 直接写代码）
  2. 拆分为原子功能点
  3. 确认用户如何验证第一个功能点

每次完成后:
  1. 按交付模板告知用户
  2. 提供具体验证步骤
  3. 等待用户反馈

用户反馈问题后:
  1. 立即停新功能
  2. 定位 → 修复 → 验证
  3. 确认修复后再继续
```

---

> 创建: 2026-06-08 | 更新: 2026-06-08
> 维护规则: 每次遇到并解决新问题后，按模板追加；发现反模式立即记录

## 归档

条目超过 30 条后，将旧条目移至 [PITFALLS-archive-2026Q2.md](PITFALLS-archive-2026Q2.md)。目前归档阈值未达到，归档文件已预创建待用。
